services:
  - type: web
    name: pilates-chat-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: OXLO_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: "*"
